/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "merakiui.com", "images.unsplash.com"], // Add domains here
  },
};

export default nextConfig;
